namespace LegacyApiProxy.Models;

/// <summary>
/// Example DTO — replace fields with what the legacy site actually exposes.
/// </summary>
public sealed record UserInfoDto(string Name, string Email);
